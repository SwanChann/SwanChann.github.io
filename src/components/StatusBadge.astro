---
type Status = 'seed' | 'growing' | 'mature' | 'evergreen';

type Props = {
  status: Status;
};

const { status } = Astro.props;
const labels: Record<Status, string> = {
  seed: '萌芽',
  growing: '生长中',
  mature: '成熟',
  evergreen: '常青',
};
---

<span class={`status status-${status}`} title={`数字花园状态：${labels[status]}`}>
  <span aria-hidden="true">●</span> {labels[status]}
</span>
